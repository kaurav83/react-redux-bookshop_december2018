import {

} from '../actions/actionTypes';

const initialState = {};

export default function cart(state = initialState, action) {
    switch(action.type) {

        default:
            return state;
    }
} 